{{ form_start(form, {'attr': {'class': 'card p-3 mx-auto my-auto mt-5 d-flex align-items-center justify-content-center flex-column', 'style': 'width: 30rem;'}}) }}

    <div class="mb-3">
        {{ form_row(form.title, {'attr': {'class': 'form-control', 'style': 'width: 25rem'}}) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.description, {'attr': {'class': 'form-control', 'style': 'width: 25rem'}}) }}
    </div>
    <div class="d-flex align-items-center justify-content-center gap-5">
    <div class="mb-3">
        {{ form_row(form.start, {'attr': {'class': 'form-control', 'style': 'width: 100%'}}) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.end, {'attr': {'class': 'form-control', 'style': 'width: 100%'}}) }}
    </div>
    </div>

    <div class="d-grid col-12 mx-auto mt-5">
            <a href="{{ path('app_home') }}" class="btn  btn-primary">{{ button_label|default('Valider') }}</a>                
    </div>
    
{{ form_end(form) }}